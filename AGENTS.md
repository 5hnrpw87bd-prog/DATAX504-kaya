# DATAX504 Assignment 1

## 对话偏好
- 请用简洁的技术语言回答
- 如果我不明确指定 /build 或 /plan，默认用 @explore 模式
- 修改代码前请先给出方案让我确认
- 回答问题时，优先引用代码位置和文件名

## 权限设置
- 我有权限读取当前目录的所有 .ipynb 文件
- 我需要你直接读取文件内容，不要问我能不能访问
- 当我说"分析 xxx 文件"时，默认使用 read_file 工具

## 🚨 绝对禁止
- ❌ 不能用 Keras、TensorFlow、PyTorch
- ❌ 不能用 sklearn 的神经网络
- ❌ 不能修改网络架构（必须是 2→16→1）
- ❌ 不能改 epochs（必须是 100）

## ✅ 必须完成
- 训练损失曲线（绘图）
- 计算参数数量（必须是 65）
- 填写反思报告（~200字）
- 截图 OpenCode 使用过程

## 📖 理解重点
如果我要修改某个函数，请先解释当前的实现逻辑，再给出修改方案。

## 🧪 验证方法
每次修改代码后，建议运行完整的 notebook 检查输出。
